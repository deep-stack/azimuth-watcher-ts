query getForfeited($blockHash: String!, $contractAddress: String!, $_participant: String!){
    getForfeited(blockHash: $blockHash, contractAddress: $contractAddress, _participant: $_participant){
        value
        proof{
            data
        }
    }
}